#!/usr/bin/env bun

// Execute the published CLI entrypoint through Bun so linked and installed
// consumers can invoke `check-suite` via the declared package binary.

import { main } from "../src/cli/index.ts";

void main();
